import React from "react";
import { Box } from "@mui/material";
type Props = {};

const Header = (props: Props) => {
  return (
    <Box className="bg-heroImgMobile md:bg-heroImgDesktop h-40 bg-cover" />
  );
};

export default Header;
